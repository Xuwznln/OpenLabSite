# 默认不需要任何配置：页面直连本机后端 http://127.0.0.1:8002（unilab --port，CORS 已放开）。

# 可选：后端不在本机且浏览器直连不到（经本机隧道 / HTTPS 混合内容）时，
# 让 Vite dev server 把同源 /api 转发过去；配置后页面默认地址变为页面自身。
# 该值不会暴露给浏览器。本机开发请保持注释。
# OPENLAB_EDGE_PROXY_TARGET=http://remote-host:8002

# 可选：把首次连接的默认地址换成别的后端（用户在页面里改过后以 localStorage 为准）。
# VITE_DEFAULT_EDGE_URL=http://127.0.0.1:8002

# 可选：驱动包索引地址（「驱动包」页的官方目录，浏览器直接读取；fork / 内网镜像时改这里，
# 页面里也能临时换）。默认 awesome-lab-devices 仓库的 main/index.json。
# VITE_OPENLAB_DEVICE_INDEX_URL=https://raw.githubusercontent.com/Xuwznln/awesome-lab-devices/main/index.json

# 可选：前端站点索引地址（顶栏「站点」目录，浏览器直接读取；fork / 内网镜像时改这里）。
# 默认 awesome-lab-sites 仓库的 main/index.json。本站固定为通用版，不读取学科主题参数。
# VITE_OPENLAB_SITE_INDEX_URL=https://raw.githubusercontent.com/Xuwznln/awesome-lab-sites/main/index.json
